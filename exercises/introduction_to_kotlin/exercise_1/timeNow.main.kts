package com.skillerwhale.main

// TODO: Import the java.time.LocalDate package

val currentDate = // TODO: Call the `now` method on `LocalDate`

fun printDate(date: LocalDate) {
	println("The date is: $date")
}

fun main() {
	val name: String = "Skiller Whale"
	println("Hello $name")
	// TODO: Call `printDate` with the `currentDate` variable
}

main()
